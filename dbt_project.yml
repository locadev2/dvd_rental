name: 'dwh'
version: '1.0.0'
profile: 'dwh'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dwh:
    staging:
      +materialized: view

    intermediate:
        +materialized: view  

    facts:
        +materialized: incremental

    dimensions:
        +materialized: table        
      
vars:
  start_date: '2005-01-01'
  end_date: '9999-12-31'